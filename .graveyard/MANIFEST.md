# Frontier Alpha Graveyard

| File | Original Path | What it provided | Why archived |
|------|--------------|------------------|--------------|
| prd-ux-optimization.json | tasks/prd-ux-optimization.json | UX optimization round 1 — 9 stories, all marked done | Completed and superseded by prd-ux-optimization-r2.json |
| prd-v2-completion.json | tasks/prd-v2-completion.json | V2 completion — 16 stories, all marked done | Completed and superseded by prd-v2-world-class.json |
| broker/ | src/broker/ | Original broker adapter layer (BrokerAdapter.ts + AlpacaAdapter.ts, 688 lines) | Superseded by src/trading/ which has 2x the features (1381 lines). Zero imports across codebase |
| NotificationSettings-settings.tsx | client/src/components/settings/NotificationSettings.tsx | Email-based alert settings (335 lines) | Duplicate — only components/notifications/NotificationSettings.tsx is imported. Settings version is dead code |
| FactorDriftMonitor.ts | src/core/FactorDriftMonitor.ts | Stateful factor-drift engine (299 lines): persistent targets (`setTargets`/`updateTarget`/`removeTarget`), drift computation (`checkDrift`, `getDriftSummary`), alert generation with in-memory history (`generateAlerts`, `getAlertHistory`), and config round-trip (`exportConfig`/`importConfig`) | Superseded by the stateless implementation inside `src/routes/alerts.ts`, which defines its own local `checkDrift` + `generateAlerts` over `DEFAULT_TARGETS`/`STRATEGY_PRESETS` and serves both `GET` and `POST /api/v1/alerts/factor-drift`. The class had zero imports anywhere in src/ or tests/. **To reconnect:** it is the only implementation with target persistence and alert history — if drift targets ever need to be stored per user (rather than posted in the request body) or drift alerts need a queryable history, restore this file and back `setTargets`/`getAlertHistory` with a Supabase table instead of instance state. |
